# Custom Agent Instructions

- **Tự động sửa lỗi (Auto-fix errors)**: Khi phát hiện bất kỳ lỗi runtime, lỗi cú pháp, lỗi logic, hoặc thông báo lỗi từ hệ thống/người dùng, AI chủ động phân tích nguyên nhân gốc rễ và tự động sửa lỗi ngay lập tức trong codebase, sau đó kiểm tra và biên dịch lại ứng dụng mà không cần chờ hỏi thêm xác nhận từ người dùng.
- **Bảo toàn tính toàn vẹn hệ thống**: Đảm bảo tất cả các tính năng hiện tại (phân quyền tài khoản, quản lý dãy phòng giường, quét OCR CCCD, nhập/xuất Excel, sao lưu phục hồi JSON, đồng bộ Firebase Cloud Firestore) luôn hoạt động ổn định và chính xác.
